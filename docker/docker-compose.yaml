# MedrixFlow Production Environment
# Usage: make up
#
# Services:
#   - nginx:       Reverse proxy (port 6200, configurable via PORT env var)
#   - frontend:    Next.js production server
#   - gateway:     FastAPI Gateway API
#   - langgraph:   LangGraph production server (Dockerfile generated by langgraph dockerfile)
#   - provisioner: (optional) Sandbox provisioner for Kubernetes mode
#
# Key environment variables (set via environment/.env or scripts/deploy.sh):
#   MEDRIX_FLOW_HOME                   — runtime data dir, default $REPO_ROOT/backend/.medrix-flow
#   MEDRIX_FLOW_CONFIG_PATH            — path to config.yaml
#   MEDRIX_FLOW_EXTENSIONS_CONFIG_PATH — path to extensions_config.json
#   MEDRIX_FLOW_DOCKER_SOCKET          — Docker socket path, default /var/run/docker.sock
#   MEDRIX_FLOW_REPO_ROOT              — repo root (used for skills host path in DooD)
#   BETTER_AUTH_SECRET               — required for frontend auth/session security
#   MEDRIX_FLOW_ENV=production       — required so Python services enforce production safeguards
#   MEDRIX_FLOW_UI_PASSWORD          — required to unlock the web UI and nginx-protected APIs
#   MEDRIX_GATEWAY_ADMIN_TOKEN       — optional extra API token for scripted/admin access
#
# LangSmith tracing is disabled by default (LANGCHAIN_TRACING_V2=false).
# Set LANGCHAIN_TRACING_V2=true and LANGSMITH_API_KEY in .env to enable it.
#
# Access: http://localhost:${PORT:-6200}

services:
  # ── Reverse Proxy ──────────────────────────────────────────────────────────
  nginx:
    image: nginx:alpine
    container_name: medrix-flow-nginx
    ports:
      - "${PORT:-6200}:6200"
    volumes:
      - ./nginx/${NGINX_CONF:-nginx.conf}:/etc/nginx/nginx.conf:ro
    depends_on:
      - frontend
      - gateway
      - langgraph
    networks:
      - medrix-flow
    restart: unless-stopped

  # ── Frontend: Next.js Production ───────────────────────────────────────────
  frontend:
    build:
      context: ../
      dockerfile: frontend/Dockerfile
      target: prod
      args:
        PNPM_STORE_PATH: ${PNPM_STORE_PATH:-/root/.local/share/pnpm/store}
    container_name: medrix-flow-frontend
    environment:
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      - MEDRIX_FLOW_ENV=${MEDRIX_FLOW_ENV:-production}
      - MEDRIX_FLOW_UI_PASSWORD=${MEDRIX_FLOW_UI_PASSWORD}
      - MEDRIX_GATEWAY_ADMIN_TOKEN=${MEDRIX_GATEWAY_ADMIN_TOKEN}
    env_file:
      - ../frontend/.env
    networks:
      - medrix-flow
    restart: unless-stopped

  # ── Gateway API ────────────────────────────────────────────────────────────
  gateway:
    build:
      context: ../
      dockerfile: backend/Dockerfile
    container_name: medrix-flow-gateway
    command: sh -c "cd backend && PYTHONPATH=. uv run uvicorn app.gateway.app:app --host 0.0.0.0 --port 6202 --workers 2"
    volumes:
      - ${MEDRIX_FLOW_CONFIG_PATH}:/app/backend/config.yaml:ro
      - ${MEDRIX_FLOW_EXTENSIONS_CONFIG_PATH}:/app/backend/extensions_config.json:ro
      - ../skills:/app/skills:ro
      - ${MEDRIX_FLOW_HOME}:/app/backend/.medrix-flow
      # DooD: AioSandboxProvider starts sandbox containers via host Docker daemon
      - ${MEDRIX_FLOW_DOCKER_SOCKET}:/var/run/docker.sock
      # CLI auth directories for auto-auth (Claude Code + Codex CLI)
      - type: bind
        source: ${HOME:?HOME must be set}/.claude
        target: /root/.claude
        read_only: true
        bind:
          create_host_path: true
      - type: bind
        source: ${HOME:?HOME must be set}/.codex
        target: /root/.codex
        read_only: true
        bind:
          create_host_path: true
    working_dir: /app
    environment:
      - CI=true
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      - MEDRIX_FLOW_ENV=${MEDRIX_FLOW_ENV:-production}
      - MEDRIX_FLOW_UI_PASSWORD=${MEDRIX_FLOW_UI_PASSWORD}
      - MEDRIX_GATEWAY_ADMIN_TOKEN=${MEDRIX_GATEWAY_ADMIN_TOKEN}
      - MEDRIX_FLOW_HOME=/app/backend/.medrix-flow
      # DooD path/network translation
      - MEDRIX_FLOW_HOST_BASE_DIR=${MEDRIX_FLOW_HOME}
      - MEDRIX_FLOW_HOST_SKILLS_PATH=${MEDRIX_FLOW_REPO_ROOT}/skills
      - MEDRIX_FLOW_SANDBOX_HOST=host.docker.internal
    env_file:
      - ../.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - medrix-flow
    restart: unless-stopped

  # ── LangGraph Server ───────────────────────────────────────────────────────
  # TODO: switch to langchain/langgraph-api (licensed) once a license key is available.
  # For now, use `langgraph dev` (no license required) with the standard backend image.
  langgraph:
    build:
      context: ../
      dockerfile: backend/Dockerfile
    container_name: medrix-flow-langgraph
    command: sh -c "cd /app/backend && uv run langgraph dev --no-browser --allow-blocking --no-reload --host 0.0.0.0 --port 6203"
    volumes:
      - ${MEDRIX_FLOW_CONFIG_PATH}:/app/config.yaml:ro
      - ${MEDRIX_FLOW_EXTENSIONS_CONFIG_PATH}:/app/extensions_config.json:ro
      - ${MEDRIX_FLOW_HOME}:/app/backend/.medrix-flow
      - ../skills:/app/skills:ro
      - ../backend/.langgraph_api:/app/backend/.langgraph_api
      # DooD: same as gateway
      - ${MEDRIX_FLOW_DOCKER_SOCKET}:/var/run/docker.sock
      # CLI auth directories for auto-auth (Claude Code + Codex CLI)
      - type: bind
        source: ${HOME:?HOME must be set}/.claude
        target: /root/.claude
        read_only: true
        bind:
          create_host_path: true
      - type: bind
        source: ${HOME:?HOME must be set}/.codex
        target: /root/.codex
        read_only: true
        bind:
          create_host_path: true
    environment:
      - CI=true
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      - MEDRIX_FLOW_ENV=${MEDRIX_FLOW_ENV:-production}
      - MEDRIX_FLOW_HOME=/app/backend/.medrix-flow
      - MEDRIX_FLOW_CONFIG_PATH=/app/config.yaml
      - MEDRIX_FLOW_EXTENSIONS_CONFIG_PATH=/app/extensions_config.json
      - MEDRIX_FLOW_HOST_BASE_DIR=${MEDRIX_FLOW_HOME}
      - MEDRIX_FLOW_HOST_SKILLS_PATH=${MEDRIX_FLOW_REPO_ROOT}/skills
      - MEDRIX_FLOW_SANDBOX_HOST=host.docker.internal
      # Disable LangSmith tracing — LANGSMITH_API_KEY is not required.
      # Set LANGCHAIN_TRACING_V2=true and LANGSMITH_API_KEY in .env to enable.
      - LANGCHAIN_TRACING_V2=${LANGCHAIN_TRACING_V2:-false}
    env_file:
      - ../.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - medrix-flow
    restart: unless-stopped

  # ── Sandbox Provisioner (optional, Kubernetes mode) ────────────────────────
  provisioner:
    profiles:
      - provisioner
    build:
      context: ./provisioner
      dockerfile: Dockerfile
    container_name: medrix-flow-provisioner
    volumes:
      - ~/.kube/config:/root/.kube/config:ro
    environment:
      - K8S_NAMESPACE=medrix-flow
      - SANDBOX_IMAGE=enterprise-public-cn-beijing.cr.volces.com/vefaas-public/all-in-one-sandbox:latest
      - SKILLS_HOST_PATH=${MEDRIX_FLOW_REPO_ROOT}/skills
      - THREADS_HOST_PATH=${MEDRIX_FLOW_HOME}/threads
      - KUBECONFIG_PATH=/root/.kube/config
      - NODE_HOST=host.docker.internal
      - K8S_API_SERVER=https://host.docker.internal:26443
    env_file:
      - ../.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - medrix-flow
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6204/health"]
      interval: 10s
      timeout: 5s
      retries: 6
networks:
  medrix-flow:
    driver: bridge
