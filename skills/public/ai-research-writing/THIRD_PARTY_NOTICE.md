This skill is an original MedrixFlow compatibility wrapper inspired by the
public repository:

- https://github.com/Leey21/awesome-ai-research-writing

The upstream repository was inspected as reference material for prompt themes and
usage scenarios, but it does not expose a native MedrixFlow-compatible
`SKILL.md` package layout. This wrapper does not vendor the upstream repository's
full content.
